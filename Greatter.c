#include<stdio.h>
int main()
{

    int a,b;
    printf("enter two number");
    scanf("%d%d",&a,&b);
    if(a>b)
        printf("the given number %d is gretter",a);
    else
        printf("the given number %d is gretter",b);
    return 0;
}
